apiVersion: v1
kind: LimitRange
metadata:
  name: databases-limits
  namespace: databases
spec:
  limits:
  - default:
      cpu: "500m"
      memory: "512Mi"
    defaultRequest:
      cpu: "250m"
      memory: "256Mi"
    type: Container
